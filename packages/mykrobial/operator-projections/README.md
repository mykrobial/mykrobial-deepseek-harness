# Mykrobial operator projections

This pure package shapes real backend facts for OmniGent and either Software Factory generation. It makes proof level, route/Trace receipts, replay/rollback readiness, PRH stage, blockers, and end-user acceptance explicit. It does not render a parallel UI, create a ticket, run a model, append Trace, approve a gate, activate a service, or deploy.
